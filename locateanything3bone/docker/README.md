# Docker build context

Build **on Olares One** (native amd64), push to **ghcr.io**:

```bash
export GHCR_TOKEN=ghp_...
./locateanything3bone/scripts/build-and-push-ghcr.sh
```

Full steps: [../BUILD.md](../BUILD.md)

Do not cross-build on Mac.
