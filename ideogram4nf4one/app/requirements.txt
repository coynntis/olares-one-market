# Ideogram4 + Instant live on Diffusers main (not yet a stable PyPI feature set).
# Use archive zip — no git binary required in the pytorch image.
diffusers @ https://github.com/huggingface/diffusers/archive/refs/heads/main.zip
transformers>=5.8.0
accelerate>=1.10.1
bitsandbytes>=0.49.2
sentencepiece
outlines==1.3.0
pydantic>=2
gradio>=5.0.0
requests>=2.28
Pillow
peft>=0.19.1
# Diffusers Hub attention kernels (FA3/FA4 via flash_4_hub / _flash_3_hub) — no source build.
kernels>=0.10.0
